[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "pulse-py"
version = "0.1.0"
description = "Python SDK for Pulse event broker"
requires-python = ">=3.8"
license = { text = "Apache-2.0" }

[tool.maturin]
features = ["pyo3/extension-module"]
